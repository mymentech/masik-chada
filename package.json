{
  "name": "masik-chada",
  "private": true,
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm ci --include=dev && npm run build",
    "build:frontend": "cd frontend && npm ci --include=dev && npm run build"
  }
}
